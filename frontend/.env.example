# Vercel: set Project "Root Directory" to `frontend` so frontend/vercel.json applies.
# Local dev: backend URL. Production: your Render API origin (no trailing slash), e.g.
# VITE_API_BASE_URL=https://personashield-api.onrender.com
VITE_API_BASE_URL=http://localhost:8000
VITE_CLERK_PUBLISHABLE_KEY=
