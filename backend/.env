HUGGINGFACE_API_TOKEN=
# Optional: Supabase (falls back to SQLite)
SUPABASE_URL=
SUPABASE_KEY=
CLERK_ISSUER=
CLERK_JWKS_URL=
# Optional: only if you serve the built SPA from this process (not needed for Vercel + Render API).
# FRONTEND_DIST=/absolute/path/to/persona/frontend/dist
